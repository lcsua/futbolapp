# HTTP-only bootstrap for ACME. Replaced by the TLS vhost after certbot.
server {
    listen 80;
    server_name develop.miliga.com.ar;

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
