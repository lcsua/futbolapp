@model List<PublicWeb.Models.Public.LeagueViewModel>
@{
    Layout = "~/Views/Shared/V2/_V2Layout.cshtml";
    ViewData["Title"] = "Ligas";
}
<section>
    <h1 class="v2-team-title" style="margin-bottom:0.35rem">Ligas</h1>
    <p class="v2-team-meta" style="margin-bottom:1.25rem">
        Vista previa V2 · entrá a un equipo desde el sitio actual o usá una URL /v2/ligas/…/equipo.
    </p>

    @if (Model == null || !Model.Any())
    {
        <div class="v2-empty">No hay ligas públicas disponibles.</div>
    }
    else
    {
        <div class="v2-stack">
            @foreach (var league in Model)
            {
                <a class="v2-match-card" href="@Url.Content($"~/ligas/{league.Slug}")">
                    <strong style="display:block;font-size:1.05rem">@league.Name</strong>
                    <span class="v2-match-card__meta" style="justify-content:flex-start;margin-top:0.35rem">
                        Sitio actual · rediseño de portada en Fase 3
                    </span>
                </a>
            }
        </div>
    }
</section>
