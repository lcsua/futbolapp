@model PublicWeb.Models.Public.MatchViewModel
@{
    var finished = PublicWeb.Helpers.TeamDisplayHelper.IsFinished(Model.Status);
    var suspended = PublicWeb.Helpers.TeamDisplayHelper.IsSuspended(Model.Status);
    ViewData["BadgeSize"] = "sm";
}
<a class="v2-match @(finished ? "is-result" : "is-upcoming")" href="@Url.Content($"~/partido/{Model.Id}")">
    <div class="v2-match__row">
        <div class="v2-match__side is-home">
            <span class="v2-match__name">@Model.HomeTeam.Name</span>
            @await Html.PartialAsync("~/Views/V2/Team/_TeamBadge.cshtml", Model.HomeTeam)
        </div>
        <div class="v2-match__mid">
            @if (finished)
            {
                if (suspended)
                {
                    <span class="v2-match__score is-warn">Susp.</span>
                }
                else
                {
                    <span class="v2-match__score">@Model.HomeScore — @Model.AwayScore</span>
                }
            }
            else
            {
                <span class="v2-match__time">@PublicWeb.Helpers.TeamDisplayHelper.FormatTime(Model.Kickoff)</span>
            }
        </div>
        <div class="v2-match__side is-away">
            @await Html.PartialAsync("~/Views/V2/Team/_TeamBadge.cshtml", Model.AwayTeam)
            <span class="v2-match__name">@Model.AwayTeam.Name</span>
        </div>
    </div>
    <div class="v2-match__meta">
        <span>@PublicWeb.Helpers.TeamDisplayHelper.FormatShortDate(Model.Kickoff)</span>
        @if (!string.IsNullOrWhiteSpace(Model.FieldName))
        {
            <span>@Model.FieldName</span>
        }
    </div>
</a>
